# Django Simple Polls App

## Setup

First make your directory project 
and secondly thing is to install Django using pip and directly generate venv:

```
python3 -m pip install Django

```

Migrate the migration if needed

```
python3 manage.py migrate

```

Happy Coding :D